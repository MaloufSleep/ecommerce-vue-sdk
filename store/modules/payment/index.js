import authorizeNet from './authorizeNet'
import synchrony from './synchrony'

const modules = {
    authorizeNet,
    synchrony
}

const state = () => {
    return  {}
}

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
    state,
    getters,
    mutations,
    actions,
    namespaced: true,
    modules
}